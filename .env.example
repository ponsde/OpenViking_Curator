# ---- OpenViking (if using OV as backend) ----
# Path to your ov.conf
OPENVIKING_CONFIG_FILE=./ov.conf
# Data directory (default: ./data)
OV_DATA_PATH=./data

# ---- Optional: Connect to remote OV HTTP serve instead of embedded mode ----
# OV_BASE_URL=http://127.0.0.1:9100

# ---- LLM (review & routing) ----
# Any OpenAI-compatible endpoint works.
CURATOR_OAI_BASE=https://api.openai.com/v1
CURATOR_OAI_KEY=sk-your-api-key
# Comma-separated model fallback chains:
# CURATOR_JUDGE_MODELS=gpt-4o,gpt-4o-mini
# CURATOR_ROUTER_MODELS=gpt-4o-mini

# ---- External Search ----
# Grok is recommended (real-time web search). Any OAI-compatible endpoint also works.
CURATOR_GROK_BASE=http://127.0.0.1:8000/v1
CURATOR_GROK_KEY=your-grok-key
CURATOR_GROK_MODEL=grok-4-fast

# ---- Advanced (optional) ----
# CURATOR_SEARCH_PROVIDER=grok         # grok | oai | custom
# CURATOR_LLM_ROUTE=1                  # 1=LLM routing, 0=rule-only
# CURATOR_CAPTURE_CASE=1               # Save pipeline cases for debugging
# CURATOR_DATA_PATH=./data             # Curator working directory
# CURATOR_VERSION=0.7.0                # Metadata version tag for ingested docs
# CURATOR_CHAT_RETRY_MAX=3             # Retry times for chat calls
# CURATOR_CHAT_RETRY_BACKOFF_SEC=0.6   # Linear backoff base seconds
# CURATOR_CONFLICT_STRATEGY=auto       # auto | local | external | human
